============================================================
GUVEL OPERATIONAL — SECURITY MAP
VERSION: 1.3
============================================================

AUTHENTICATION
--------------
Provider: Supabase Auth
Frontend key: Publishable / anon key only
Service role key: NEVER exposed in GitHub or frontend

ACTIVE COMPANY CONTEXT
----------------------
Authenticated user
  -> auth.users.id
  -> company_members.user_id
  -> company_members.company_id
  -> activeCompanyId in current browser session

CUSTOMERS RLS
-------------
Table: public.customers
RLS: ENABLED
SELECT: authenticated active company member only
INSERT: authenticated active company member only
UPDATE: authenticated active company member only
DELETE: authenticated active company member only

Authorization helper:
public.is_company_member(company_id)

No anonymous development policy is used for customers.

PHASE 1.3 SECURITY STATUS
-------------------------
Customers CRUD operates only through the authenticated company context.
The frontend does not use the old manual ACTIVE_COMPANY_ID configuration.
